<!DOCTYPE html>
<html>

<head>
    <title>Редактирование роли</title>
    <meta charset="utf-8" />
</head>

<body>
    {{> header}}
    <div class="container">
        <h3>Редактирование роли пользователя {{username}}</h3>
        <div class="row">
            <div class="col-md-12">
                <div class="input-group input-group-lg">
                    <form method="POST">
                        <div class="form-outline mb-4 d-flex flex-column">
                            <label>Роль</label>
                            <select name="role" class="form-control">
                                {{#each themes}}
                                <option value='{{this}}'>{{this}}</option>
                                {{/each}}
                                <option value='admin'>Администратор</option>
                            </select>
                        </div>
                        <input type="submit" value="Сохранить" class="btn btn-primary btn-block mb-4" />
                        <div class="form-outline mb-4 d-flex flex-column">
                            <a href="/admin/users/all">Назад</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>

</html>